学校ごとのスラック作成ページ

<%= form_for(@comment, :url => { controller:'comment', action:'create'})do |f| %>
   <%= f.label :body %>
   <br>
   <%= f.text_field :body,size: 140 %>
   <br>
   <%= f.submit %>
   <br>
<% end %>